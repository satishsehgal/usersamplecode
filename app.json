{
  "name": "usersamplecode",
  "scripts": {
    "postdeploy": "pg_dump $HEROKU_POSTGRESQL_AMBER_URL | psql $DATABASE_URL && bundle exec rake db:migrate"
  },
  "environments": {
  "test": {
    // …
    "scripts": {
      "test-setup": "",
      "test": "bundle exec rake test"
    }
  },
  "staging": {
    
  }
},
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ]
}
